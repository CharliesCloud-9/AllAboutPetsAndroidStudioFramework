<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.carlosgallegos.androidphpmysql">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
 The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="@android:icon">

        <!-- android:debuggable="true" -->

        <activity
            android:name=".SplashActivity_0"
            android:launchMode="singleTop"
            android:theme="@style/AAP_Custom_CG">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity_2" />
        <activity
            android:name=".Tab1FunDapterSubActivityCat"
            android:label="Cat Symptoms"
            android:parentActivityName=".ExpListViewDictCat">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ExpListViewDictCat" />
        </activity>
        <activity android:name=".TabHostActivity_3" />
        <activity android:name=".Tab2" />
        <activity android:name=".Tab3" />
        <activity android:name=".Tab4" />
        <activity
            android:name=".SignUpActivity_2b"
            android:label="Sign up" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".ExpListViewDictCat"
            android:label="Cat Symptoms"
            android:parentActivityName=".SymptomChecker">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".SymptomChecker"/>
        </activity>
        <activity android:name=".Tab2Test" />
        <activity
            android:name=".SymptomChecker"
            android:label="Symptom Checker" />
        <activity android:name=".ExpListViewDictDog"
                android:label="Dog Symptoms"
            android:parentActivityName=".SymptomChecker">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SymptomChecker"/>
        </activity>
    </application>

</manifest>
